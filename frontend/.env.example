# In local dev, Vite proxies /api -> localhost:8000 automatically (see vite.config.js) —
# you don't need this file for local development.
#
# In production (Vercel), set this to your deployed Render backend URL, e.g.:
# VITE_API_URL=https://waypoint-backend.onrender.com
VITE_API_URL=
