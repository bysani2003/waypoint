// Single consistent brand accent for every tag/category, so the visual
// language never fragments between "marketing" and "product" screens.
const ACCENT = { bar: '#da6c3d', text: '#b8542a', tint: '#fbe9e0' }

export function colorForTag(_tag) {
  return ACCENT
}
